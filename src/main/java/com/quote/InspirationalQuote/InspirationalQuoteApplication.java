package com.quote.InspirationalQuote;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class InspirationalQuoteApplication {

	public static void main(String[] args) {
		//ApplicationContext context =
				SpringApplication.run(InspirationalQuoteApplication.class, args);
		//QuoteService quoteScraperService = context.getBean(QuoteService.class);
		//quoteScraperService.scrapeAndSaveQuotes();

		// Once the scraping and saving is done, you can exit the application

	}

}
